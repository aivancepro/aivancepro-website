---
import { getCollection, render } from 'astro:content';
import ArticleLayout from '../../../layouts/ArticleLayout.astro';

export async function getStaticPaths() {
  const posts = await getCollection('blog', ({ data }) => data.lang === 'en');
  return posts.map((post) => ({
    params: { slug: post.data.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await render(post);

const wordCount = post.body ? post.body.split(/\s+/).length : 0;

const related = (await getCollection('blog', ({ data }) =>
  data.lang === 'en' &&
  data.category === post.data.category &&
  data.slug !== post.data.slug
)).sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()).slice(0, 3);
---

<ArticleLayout
  lang="en"
  title={post.data.title}
  description={post.data.description}
  canonical={post.data.canonical}
  alternates={post.data.alternates}
  tag={post.data.tag}
  category={post.data.category}
  date={post.data.date}
  dateModified={post.data.dateModified}
  readTime={post.data.readTime}
  backLink="/en/blog/"
  backLabel="Back to blog"
  image={post.data.image}
  author={post.data.author}
  relatedArticles={related}
  jsonLd={[
    {
      "@context": "https://schema.org",
      "@type": "BlogPosting",
      "headline": post.data.title,
      "description": post.data.description,
      "datePublished": post.data.date,
      "dateModified": post.data.dateModified || post.data.date,
      "keywords": post.data.keywords,
      "inLanguage": "en",
      "wordCount": wordCount,
      "articleSection": post.data.tag,
      "mainEntityOfPage": {
        "@type": "WebPage",
        "@id": post.data.canonical,
      },
      "image": post.data.image || "https://aivancepro.fr/assets/og-image.png",
      "thumbnailUrl": post.data.image || "https://aivancepro.fr/assets/og-image.png",
      "author": {
        "@type": "Person",
        "name": post.data.author || "Theo Stanmore",
        "url": "https://aivancepro.fr/",
      },
      "publisher": {
        "@type": "Organization",
        "name": "AIVancePro",
        "url": "https://aivancepro.fr",
        "logo": {
          "@type": "ImageObject",
          "url": "https://aivancepro.fr/logo.png",
        },
      },
    },
    {
      "@context": "https://schema.org",
      "@type": "BreadcrumbList",
      "itemListElement": [
        { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://aivancepro.fr/en/" },
        { "@type": "ListItem", "position": 2, "name": "Blog", "item": "https://aivancepro.fr/en/blog/" },
        { "@type": "ListItem", "position": 3, "name": post.data.title, "item": post.data.canonical },
      ],
    },
  ]}
>
  <Content />
</ArticleLayout>
